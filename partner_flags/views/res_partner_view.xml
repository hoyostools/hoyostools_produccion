<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_form_inherit" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.flags</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <xpath expr="//page[@name='sales_purchases']//group[@name='sale']/field[@name='user_id']"
                   position="before">
                <field name="is_customer"/>
            </xpath>

            <xpath expr="//page[@name='sales_purchases']//group[@name='purchase']/field[@name='property_supplier_payment_term_id']"
                   position="before">
                <field name="is_supplier"/>
            </xpath>

        </field>
    </record>

</odoo>