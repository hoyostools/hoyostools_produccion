<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_account_payment_tree_inherit_anticipos" model="ir.ui.view">
        <field name="name">account.payment.tree.inherit.anticipos</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_tree"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='name']" position="after">

                <field name="anticipo"/>
                <field name="tipo_anticipo_id"/>

            </xpath>

        </field>
    </record>

    <record id="view_account_payment_form_custom" model="ir.ui.view">
        <field name="name">account.payment.form.custom</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="voucher_type"/>
                <field name="tipo_anticipo_id" invisible="voucher_type != 'anticipo'" options="{'no_create': True}"/>
                <field name="destination_account_id" readonly="voucher_type == 'anticipo'" options="{'no_create': True, 'no_open': True}"/>
            </xpath>

            <xpath expr="//field[@name='partner_id']" position="before">
                <field name="partner_type"/>
            </xpath>

        </field>
    </record>

</odoo>