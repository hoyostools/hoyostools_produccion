<odoo>
    <record model="ir.ui.view" id="view_invoice_form">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='ref']" position="attributes">
                <attribute name="invisible">move_type not in ('in_invoice', 'in_receipt', 'in_refund', 'out_refund')</attribute>
            </xpath>
            <xpath expr="//label[@for='ref']" position="attributes">
                <attribute name="invisible">move_type not in ('in_invoice', 'in_receipt', 'in_refund', 'out_refund')</attribute>
            </xpath>
        </field>
    </record>

    <record model="ir.ui.view" id="view_out_invoice_tree_inherit_payment_term">
        <field name="name">account.move.out.invoice.tree.payment.term</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_out_invoice_tree"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='invoice_date_due']" position="after">
                <field name="invoice_payment_term_id" optional="hide"/>
            </xpath>

        </field>

    </record>
</odoo>