<odoo>
    <record id="view_purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.box.quantity</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='order_line']/tree/field[@name='analytic_distribution']" position="after">
                <field name="caja_interna_qty" string="Caja Interna" widget="integer" optional="hide"/>
                <field name="caja_externa_qty" string="Caja Externa" widget="integer" optional="hide"/>
            </xpath>
        </field>
    </record>
</odoo>