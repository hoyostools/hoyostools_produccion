<odoo>
    <data>

        <record id="product_template_form" model="ir.ui.view">
            <field name="name">product.template.form.inherit.compra</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_only_form_view"/>
            <field name="arch" type="xml">

                <!-- Agregar grupo COMPRA antes de seller_ids -->
                <xpath expr="//page[@name='purchase']//field[@name='seller_ids']" position="before">

                    <group string="COMPRA">

                            <field name="orden_compra" widget="many2many_tags"/>
                            <field name="fecha_recepcion"/>

                    </group>

                </xpath>

            </field>
        </record>

        <record id="product_template_search" model="ir.ui.view">
            <field name="name">product_template_search</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_search_view"/>
            <field name="arch" type="xml">

                <xpath expr="//search" position="inside">
                    <field name="orden_compra"/>
                    <filter
                        name="fecha_recepcion"
                        date="fecha_recepcion"
                        string="Fecha de Recepción"/>
                </xpath>

            </field>
        </record>

    </data>
</odoo>